# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: Name
        searchable: true
    slug:
        label: Slug
        searchable: true
    posts_count:
        label: Count
        relation: posts
        useRelationCount: true
